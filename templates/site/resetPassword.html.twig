{% extends 'site.html.twig' %}
{% trans_default_domain 'reset_password' %}

{% block title %}{{ 'reset.title'|trans }}{% endblock title %}

{% block content %}
    <div class="form-card-wrapper">
        <div class="uw-card">
            <div class="uw-card-header">{{ 'reset.header'|trans }}</div>
            <div class="uw-card-body">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>
{% endblock %}
