{% extends 'game.html.twig' %}

{% block title %}Ultimate Warfare - Build region{% endblock title %}

{% block content %}
    Go to: <a class="B" href="{{ path('Game/WorldMap') }}">World Map</a>
    {% for gameUnitCategory in gameUnitCategories %}
        - <a class="B" href="{{ path('Game/World/Region/RemoveGameUnits', {'regionId': region.id, 'gameUnitCategoryId': gameUnitCategory.value}) }}">{{ gameUnitCategory.label }}</a>
    {% endfor %}

    <form method="post" action="{{ path('Game/World/Region/RemoveGameUnits', {'regionId': region.id, 'gameUnitCategoryId': gameUnitCategory.value}) }}">
        <table class="table text-center">
            <tr class="tabletop">
                <td colspan="4"><b>Destroy Units</b></td>
            </tr>
            <tr>
                <td></td>
                <td>Unit</td>
                <td>You have</td>
                <td>Amount</td>
            </tr>
            {% for gameUnit in gameUnits %}
                <tr>
                    <td width="150">
                        <img src="{{ asset('images/' ~ gameUnit.gameUnitCategory.imageDir ~ gameUnit.image) }}" alt="{{ gameUnit.name }}" width="150" height="130">
                    </td>
                    <td width="150">
                        <a class="B" href="{{ path('Guide/GameUnit', {'gameUnitId': gameUnit.gameUnitEnum.value}) }}" target="_blank" title="Information about {{ gameUnit.name }}" tabIndex="-1">
                            <b>{{ gameUnit.name }}</b>
                        </a><br />
                    </td>
                    <td width="100">
                        {% if gameUnitData[gameUnit.gameUnitEnum.value] is defined and gameUnitData[gameUnit.gameUnitEnum.value] > 0 %}
                            {{ gameUnitData[gameUnit.gameUnitEnum.value] }}
                        {% else %}
                            0
                        {% endif %}
                    </td>
                    <td width="200">
                        <input type="text" class="form-control" name="destroy[{{ gameUnit.gameUnitEnum.value }}]" />
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="4">No Units in the UW database!</td>
                </tr>
            {% endfor %}
            <tr>
                <td colspan="4">
                    <input class="btn btn-primary" type="submit" name="submit" value="Destroy">
                </td>
            </tr>
        </table>
    </form>
{% endblock %}
