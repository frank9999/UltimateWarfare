{% extends 'admin.html.twig' %}

{% block title %}ADMIN - Game news{% endblock title %}

{% block content %}
    <div class="uw-card">
        <div class="uw-card-header">Edit game news</div>
        <div class="uw-card-body">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
